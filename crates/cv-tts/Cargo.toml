[package]
name    = "cv-tts"
version = "0.1.0"
edition = "2024"

[dependencies]
cv-core = { path = "../cv-core" }

[dependencies.windows]
workspace = true
features = [
    "Win32_System_Com",
    "Win32_Media_Speech",
    "Win32_UI_Accessibility",
]
